/* eslint-disable */
(function() {
  // const reloadFunction = async () => {
  // console.log('start', ~~(Math.random() * 100));
  // try {
  //   let about = await fetch('http://localhost:3001/status-reload');
  //   about = about.text();
  //   console.log(about);
  // } catch (e) {}
  // if (about.status === 200) window.location.reload(false);
  // };
  // setTimeout(reloadFunction, 500);
})();
